pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // library progressdialog butuh ini
        maven { url "https://jitpack.io" }

    }
}
rootProject.name = "KP"
include ':app'
